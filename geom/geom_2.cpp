#include <iostream>
#include <math.h>
using namespace std;

int main()
{
    int n=0;
    float sum =0, r=0, itog=0;
    cout << "Введите количество вершин: ";
    cin >> n;
    float v[n*2];
    cout << "Введите координаты каждой из вершин";
    for (int i=0; i*2<n ; i++)
    {
        cin >> v[i];
    }
    for (int i=0; i<n-2 ; i++)
    {
        r=i[]
        //NESDELANO
    }
    cout << "Периметр: " << sum;
    return 0;
}